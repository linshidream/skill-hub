# Skill 发布记录

本文件维护 skill 的完整增量发布历史。每个新增、更新、废弃、修复或文档变更都保留一条记录；去重后的当前版本清单见 [SKILL_CURRENT.md](SKILL_CURRENT.md)。

## 维护规则

- 新增、重命名、废弃或发布新版本 skill 时，必须更新本文件。
- 同步更新 [SKILL_CURRENT.md](SKILL_CURRENT.md) 和 `registry.json` 中的当前版本信息。
- 本文件可以出现同一 skill 的多个版本记录，不做去重。
- 发布时间使用 `YYYY-MM-DD HH:MM +0800` 格式。
- 变更类型使用：`新增`、`更新`、`废弃`、`修复`、`文档`。

## 增量表

| 发布时间 | Skill | 版本 | 分类 | 变更类型 | 状态 | 支持 Agent | 变更摘要 | 入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-14 11:10 +0800 | `dev-lifecycle` | `0.1.7` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | dev-lifecycle 正式接管前端 cascade——schema 与 resolver 对前端工程（web-pc/taro-mobile）与 Java 工程等价：①`dev-flow.schema.json` scaffold 块——template enum 扩 `web-pc`/`taro-mobile`，新增 `language`/`build-tool`/`versions`（前端依赖版本快照子对象，additionalProperties string）字段，`additionalProperties:false` 严格化（防字段漂移）；build-credentials 块新增前端凭据 `weapp-appid`/`weapp-upload-key-id`/`static-host-test`/`static-host-prod`（占位不进仓库）；②`project-state.schema.json` scaffold 块——template enum 同步扩 `web-pc`/`taro-mobile`，新增 `language`/`build-tool`/`versions` 字段，`additionalProperties:false` 严格化；③`resolve-active-state.py`——`init-scaffold --template` choices 扩 `web-pc`/`taro-mobile`（不再 invalid choice），`set-scaffold-phase` 新增 `--language`/`--build-tool` 参数并写入 `project.json` scaffold。消除 0.1.6 仅认 Java 导致前端 .dev-flow.yml/project.json 10 字段 schema 不匹配隐患。验证：taro-mobile init-scaffold + set-scaffold-phase --language typescript --build-tool pnpm 写入成功；web-pc 生成产物 .dev-flow.yml + project.json 三 schema（dev-flow scaffold/build-credentials + project-state scaffold）全绿；java-web 回归零 diff。 | `skills/dev/dev-lifecycle` |
| 2026-09-14 11:10 +0800 | `project-init` | `0.4.1` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | 修复 0.4.0 前端产物与 dev-lifecycle 0.1.7 schema 不匹配（前端 .dev-flow.yml/project.json 有 10 个 schema 未声明字段被 additionalProperties:false 拒）：①`merge.py` `generate_dev_flow` 前端分支——版本字段（taro/nutui/react/antd/vite/tailwind/typescript .version）从 scaffold 顶层平铺改为 `scaffold.versions` 子对象（方案 A），与 dev-flow.schema.json 对齐；②`write_project_state` 前端分支——`run_best_effort`→`run_strict`（与 Java 路径等价，失败即 exit 而非 WARN 跳过），`set-scaffold-phase` 传 `--language`/`--build-tool`（从 base manifest 的 project 块读，不硬编码）；函数签名增 `lang`/`build_tool` 参数。0.4.0 的"前端 best-effort + F4 二期"已知限制解除（dev-lifecycle 已正式接纳前端，不再需要独立 taro-frontend.yml）。验证：web-pc 生成 .dev-flow.yml scaffold.versions 子对象结构正确 + project.json 写入 language/build-tool + 三 schema 全绿；java-web 回归零 diff（scaffold 仍 java-version/boot-version 形态）。 | `skills/dev/project-init` |
| 2026-08-18 18:47 +0800 | `project-init` | `0.4.0` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 后端"三段式标准化闭环"（scaffold固化+版本固化+flow固化）迁移到前端，新增前端工程生成能力（java-web/java-mcp 向后兼容零回归）：①`merge.py` 泛化——`project_family()` 按前缀判族（java-→java，web-/taro-/rn-→frontend），`base_mixin_dir()` 按族路由 base-mixin（java→java-maven-base，frontend→frontend-common），`compat_entry_for` 表驱动扫描 compat[project_type]→frontend-shared（前端）→tech-pref，`run_version_check` 按 kind 路由 maven(version-check.sh 解析 maven-metadata.xml) 或 npm(version-check-npm.sh 解析 registry.npmjs.org JSON)，新增 `generate_package_json()` 聚合各层 npm 依赖片段注入 package.json（精确锁无 `^`，merge_scripts 合并 scripts），`_generate_frontend()` 前端单工程生成分支，`cmd_project()` --mode project 多工程组织壳（父目录仅 README，各子工程独立 pkg/技术栈，默认无 monorepo/workspace/shared）；②新增 `validators/version-check-npm.sh`——scoped 包 `@scope/pkg`→`@scope%2Fpkg` URL 编码，过滤含 `-` 预发布，series-prefix 筛最新稳定 GA，sort -V 取最大；③`compat-table.yml` 重写为表驱动——compat.{java-web,java-mcp,web-pc,taro-mobile,frontend-shared}，每 entry 含 variable/series/artifact/kind；④新增 `mixins/frontend-common/`（端无关骨架）——tsconfig strict + eslint flat config（import-boundary 两层锁：①h5/weapp 禁 antd/@ant-design/*，②非 pc 模块禁 @/components/platform/pc + @/pages/platform/pc-admin）+ prettier + tailwind.preset + design-token（中性色零业务名）+ 横切分层骨架（api/service/types/utils/hooks/components/pages 各 business/platform 叶子层）+ 规约双轨（eslint 机器可读为源 + docs/frontend-spec.md 人读镜像，scripts/check-spec-sync.js 校验【E】规约编号同源不漂移，27 条规约中 6 条【E】机器强制 21 条【M】人工）+ .npmrc(save-exact=true)；⑤新增 `templates/web-pc/`（vite5+React18+antd5.22+tailwind3.4 纯 PC web，非 Taro）；⑥新增 `templates/taro-mobile/`（Taro3.6+NutUI3.0.20 多端 H5+微信小程序，weapp-tailwindcss 3.x，全系 @tarojs/* 共享 taro.version 防错配）；⑦新增 `mixins/state-business/`（可选，TanStack Query 服务端态 + Zustand 客户端态双层，include.state-business=y 开启，默认关零副作用）；⑧新增 `mixins/jenkins-frontend-ci/`（H5 静态 rsync 部署 + 小程序 miniprogram-ci 上传，无镜像无 docker）——Jenkinsfile 触发时机归 ci-trigger/dev-lifecycle，check-build-ready.sh L0 规约同源+L1 ci-trigger+L2 jenkins credentials API 验引用存在+appid 占位已填+L3 静态服务器可达，凭据/appid/私钥全 REPLACE_WITH_* 占位不进仓库（同 Java 凭据红线）；⑨`generate_dev_flow`/`write_project_state` 按族分支——Java 严格走 resolver(java-web/java-mcp)，前端 best-effort（resolver 暂不识别前端 template，WARN 跳过 project.json 写入，F4 二期建 taro-frontend.yml）。版本基线 LOCKED：Taro3.6+NutUI3.0.20（npm 查证 @tarojs/taro 3.6.40 / @nutui/nutui-react-taro 3.0.20）。已知限制（如实标注）：前端 V1 复用 java schema 的 resolver 为 best-effort（不严格），二期建专属 taro-frontend.yml；Taro 微信小程序全量构建冒烟延迟到首个落地项目（本地无微信开发者工具 CI 环境验证）；rn-app template 为二期占位（未实现）。验证：web-pc/taro-mobile 单工程生成全版本解析无残留占位、package.json 精确锁无 `^`、--mode project 多工程组织壳结构正确、java-web 回归零 diff（仅时间戳差异）、脱敏扫描前端产物层零真实凭据（42 处 REPLACE_WITH_* 占位）。 | `skills/dev/project-init` |
| 2026-08-08 16:20 +0800 | `project-init` | `0.3.0` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 可选数据源 + 健康检查 SPI + 配置环境分层（java-web / java-mcp 均覆盖，向后兼容）：①新增三个可选数据源 mixin（`mixins/mysql`/`mixins/redis`/`mixins/rocketmq`），经初始化表单 `include.{mysql,redis,rocketmq}` 开关条件加载（默认全启用，填 n 关闭）；禁用则 mixin 不加载——`provides.files` 与 pom 片段均不进生成图，零副作用（非"生成后删除"）；叠加顺序 base < [data-source] < tech-pref < template < ci-type；mysql=HikariCP+jdbc+`com.mysql:mysql-connector-j`（版本由 Boot parent 管理，2.7.18=8.0.33/3.5.x 同名新坐标），redis=`org.redisson:redisson` 3.13.6 核心包（非 starter，手写 `RedissonConfig` 单机 bean，无密码不调 setPassword），rocketmq=`rocketmq-spring-boot-starter` 2.2.3（Boot2 定制）；redisson/rocketmq 版本为 2026-08-08 钉查证 GA 值（注释注明来源+日期，voucher-ledger 实测全绿），未走 version-check（后续可加 compat-table 条目）；②HealthChecker SPI：base 提供 `HealthChecker` 接口（name/check/rootCause default + Status enum，java8 兼容），各数据源 mixin 提供 `*HealthChecker`（@Component，注入对应 bean `required=false`），java-web `HealthController` 改注入 `List<HealthChecker>` 自动聚合——天然条件性（有数据源才有 Checker bean，无则该项缺省，整体仍 UP），三原则落地（有才检查/单项异常不影响其他/响应完整诊断，整体 status 全 UP/SKIPPED=UP 否则 DEGRADED 不报 DOWN）；mysql 查 `SELECT COUNT(*) FROM t_health_check`（表 DDL+种子随 mixin 生成到 `docs/sql/health-check.sql`），redis 查预留键 `health:check`（连通即 UP，键缺失 warning，连不上 DOWN），rocketmq 查 `fetchPublishMessageQueues("health-check")` topic 路由（查到路由→UP，失败→DOWN；API 限制：`fetchPublishMessageQueues` 只抛 MQClientException，nameserver 不可达与 topic 无路由无法区分，失败统一 DOWN 保守降级，偏离原评审异常分类方案）；③配置环境分层：`application.yml` 改核心极简（active=local+application.name+log-router import+server.port），新增 `application-local.yml`（env=local+DEBUG，本地写死值），test/prod 去 application.name/config.import/port（移到 application.yml）留 env+logging；数据源配置经 mixin 的 `extra-config`（`spring:` 子级，如 mysql datasource）或 `top-config`（顶层，如 redis/rocketmq）注入环境文件 `@@SPRING-EXTRA@@`/`@@TOP-EXTRA@@` 双占位——三环境文件都注入同一数据源块，值 `${ENV:本地默认}`（local 用默认，test/prod 环境变量 `MYSQL_*`/`REDIS_*`/`ROCKETMQ_*` 覆盖）；双占位设计解决多 mixin 共享 `spring:` 顶层键（mysql datasource + java-mcp ai 同为 spring 子级，无 DuplicateKey）与 redis/rocketmq 顶层键分离；logback `<springProfile name="test">`→`test | local`（local 复用 test 日志配置）；④`merge.py`：`load_layer` 加 `data-source` kind，`_DS_DEFAULTS`+条件加载 `ds_layers`（manual/--var 优先，ds 变量用 setdefault），`pom_layers` 注入 ds manifests，`generate_files` 加 `top-config`/`@@TOP-EXTRA@@` 注入，`SKILL_VERSION`→0.3.0，摘要加 data-sources/redisson/rocketmq-spring；`--add-module` 增量模块不加载 data-source mixin（库模块场景，保持现状）。已知限制（如实标注）：rocketmq-spring 2.2.3 基于 Boot2/javax，在 java-mcp（Boot3.5/jakarta）下编译通过但 autoconfigure 运行时可能因 jakarta 命名空间报错（编译≠运行兼容），java-mcp 启用 rocketmq/redis 后需运行冒烟；java-mcp 无 HealthController（MCP server，Checker 仍生成 @Component 可被 MCP tools 调用但不聚合 /health）；运行连通性需部署后 GET /health 验证（mysql 表已建+INSERT、redis 预留键已 SET、rocketmq nameserver 可达）。验证：三场景（java-web 全启用 / java-web 全禁用 / java-mcp 全启用）生成 + 文件落位 + YAML 结构正确。local 写死值仅本地，test/prod ${ENV} 守 P0 红线。 | `skills/dev/project-init` |
| 2026-07-16 21:45 +0800 | `project-init` | `0.2.0` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 补 3 项能力缺口（java-web + java-mcp 均覆盖）：①build.sh 去 `--pull`——默认用本地 base 镜像（离线/受限网络节点已 docker load 仍被 --pull 强制走 Docker Hub 超时卡死），env `PULL=true` 可重启用；②新增 `scripts/docker-images-offline.sh` 离线镜像分发（local 本机打包 amd64 镜像成 bundle → 跳板机上传 → server 按需 load 缺失镜像），每镜像独立存档+image-list 清单+架构校验(amd64)+幂等+兼容旧 tar；镜像清单按所选 template 的 java 版本派生（`{{docker.build.image}}`/`{{docker.run.image}}`，java-web 2 个/java-mcp 2 个，非硬编码 4 个）；③新增 `scripts/README.md` 索引文档（速查表+各脚本详述+约定，crontab 路径用 `{{deploy.root}}`）。附带修复：manifest java8 运行镜像 `openjdk:8-jre-alpine`→`eclipse-temurin:8-jre-alpine`（openjdk:8-jre-alpine 已被 Docker Hub 弃用移除整库 403，Dockerfile FROM + 离线脚本一致化）。验证：两模板生成产物 build.sh 无 --pull、离线脚本镜像清单按版本派生、Dockerfile FROM=temurin、scripts/README 落位。 | `skills/dev/project-init` |
| 2026-07-14 10:17 +0800 | `project-init` | `0.1.9` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | 修复 run.sh/rollback.sh 同位 rm 漏删同名容器 bug：0.1.8 的同位模糊删用 `grep -v "^${CONTAINER}$"` 排除当前 version 容器，意图"保留当前 version"，却导致重跑同 version 同 port 时（最常见场景，如重试构建）同名旧容器被排除不删 → `docker run` 报 `Conflict. The container name ... is already in use`。修复：去掉 `grep -v`，删同位所有（含当前 version 同名）——docker run 复用 CONTAINER 名，同名必须先删。教训：rm 测试用例必须覆盖"同名重复部署"，不能只测"不同 version 替换"。验证：mock 同 version 同名场景——删同名 v1.0.0 + 旧 v0.9.0，保留不同 port + 无关。 | `skills/dev/project-init` |
| 2026-07-13 19:09 +0800 | `project-init` | `0.1.8` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 容器命名体系重做（HOST_PORT 当节点 id + version 进容器名 + 同位替换 rm）：①容器名改 `{project}-{active}-{version}-{hostid}-{host_port}`，HOSTID=$(hostname -s) 自动取宿主机短名，去 NODE_ID（端口天然唯一做节点标识，NODE_ID 冗余）；②version 进容器名后 rm 改同位模糊删——docker ps grep 同 project+active+hostid+host_port 但 version 不同（.+ 通配）的旧容器删除，保留不同 port+无关+当前 version（grep -v），实现新 version 同位替换/蓝绿过渡；③run.sh 4 参（去 node_id）、rollback.sh 3 参；摘要与 HISTORY 记 hostid+host_port（去 node）；④Jenkinsfile 加 HOST_PORT choice 选项——按约束从初始化表单 server.port 派生两端口（server.port 与 server.port+1，merge.py 计算 server.port.next），非硬编码 10001/10002；表单 server.port 仍为用户输入不改；⑤build-readiness B 节补多实例说明。验证：mock 同位 rm 正确删旧 version 同 port、保留不同 port+无关；Jenkinsfile choice=['8700','8701']（server.port=8700 派生）；无 NODE_ID 残留。 | `skills/dev/project-init` |
| 2026-07-13 17:36 +0800 | `project-init` | `0.1.7` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | 修复 java-web 模板 Java 8 构建失败：manifest 的 `maven.compiler.release: "{{java.version}}"` 导致 javac 8 报 `Fatal error compiling: invalid flag: --release`（`--release` 是 Java 9+ flag，java-web 构建容器 maven:3-alpine=Java 8 不识别）。改为 `maven.compiler.source`+`maven.compiler.target`（全版本兼容）。SKILL.md 第 4 节"java 版本贯穿"同步更新为 source/target，并加禁用 release 的红线说明。java-mcp 未声明 release（靠 spring-boot parent 隐式映射），不受影响。验证：生成 java-web 项目 pom 含 source/target=8 无 release，mvn compile BUILD SUCCESS。 | `skills/dev/project-init` |
| 2026-07-13 17:21 +0800 | `project-init` | `0.1.6` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 运维可观测性 + Jenkinsfile checkout 策略：①build/run/rollback 启动时打印配置摘要（变量派生完、执行操作前）——registry/namespace/image/端口(SERVER_PORT vs HOST_PORT)/路径(CONFIG_DIR 宿主 vs CONTAINER_CONFIG_DIR 容器)/回滚版本一目了然，便于排查；②build.sh 的 SWR_USER/SWR_TOKEN 用 mask 函数只显示 <set>/<unset>，绝不打印 AK/SK（禁用 ${VAR:-<unset>} 判断，VAR 非空会输出值=泄密）；③Jenkinsfile checkout stage 改默认 Pipeline script from SCM 模式——git 步骤注释 + 上方备注 + echo 占位，注释行保留 {{gitee.credential.id}}/{{git.repo.url}} 变量（merge.py 注入真实值），切回内联模式放开注释即用。run/rollback 无凭据不加 mask。三脚本 bash -n OK，build.sh mock 实测摘要+mask 正确。 | `skills/dev/project-init` |
| 2026-07-13 16:31 +0800 | `project-init` | `0.1.5` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 新增 push-remote.sh：初始化后一键推送 master+test 到远程 gitee（SSH 免密）。脚本运行时从 .dev-flow.yml 读 ci.jenkins.build-credentials.git-repo-url（HTTPS）转 SSH（gitee/github 通用，已 SSH 则透传），配 remote 并 push -u 双分支；cwd 自适应（脚本在 scripts/，自动 cd 项目根）；幂等（remote remove+add 可重复，已推送 up-to-date）；覆盖保护：每分支 push 前跑 git push --dry-run，远程有本地没有的提交（non-fast-forward/远程领先）拒推并给 pull --rebase 或 -f 指引，全程无 --force。manifest provides 加引用；build-readiness.md 增 D 节（首次推送远程：配 SSH key/网页建私有仓库不勾初始化/跑脚本），原 D 改 E（首次构建验证）。无 {{var}}，模板与产物一致。bash -n + URL 转换 5 例 + 端到端生成验证通过。 | `skills/dev/project-init` |
| 2026-07-13 15:49 +0800 | `project-init` | `0.1.4` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | java-mcp 模板默认传输改 Streamable HTTP（开箱 /mcp 不再 404）：Spring AI 1.0.x auto-config 只装配 SSE（/sse+/mcp/message），无 /mcp，现代 MCP 客户端默认 Streamable HTTP → 404。新增 templates/java-mcp 专属 Application.java.tmpl（exclude McpServerAutoConfiguration+McpWebMvcServerAutoConfiguration，覆盖 base 的 plain 版，因 base 与 java-web 共用不能加 exclude）+ McpStreamableServerConfig.java.tmpl（手动装配 WebMvcStreamableServerTransportProvider 暴露 /mcp，复刻 McpServerAutoConfiguration 工具装配逻辑，并注册 ExampleTools 为 ToolCallbackProvider——解决 @Tool 不自动注册的死代码问题）；java-mcp 专属 README.md.tmpl 覆盖 base，补 MCP 端点/传输类型/curl 握手示例。模板代码与实测项目逐字节一致（仅 package 变量化），mvn compile BUILD SUCCESS。 | `skills/dev/project-init` |
| 2026-07-13 15:33 +0800 | `project-init` | `0.1.3` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | 部署路径体系修复（jenkins-docker-ci 模板）：①deploy.root 变量贯穿所有宿主机路径——compress-old-logs.sh.tmpl 的 crontab 示例与 LOG_DIR 默认值写死 /opt/app 改 {{deploy.root}}；application-prod.yml.tmpl 注释宿主路径改 {{deploy.root}}（path 容器内保留）；②容器内/宿主机路径分离——run.sh/rollback.sh 引入 CONTAINER_CONFIG_DIR=/opt/app/server/{module}（容器内固定），-v 与 PARAMS 的 additional-location 改用容器侧变量，不再 -v ${CONFIG_DIR}:${CONFIG_DIR} 同路径挂载（容器内不再出现宿主机路径如 /home/recharge/server/...）；③run.sh 的 mkdir -p 提前到 docker run 前，避免 bind-mount 让 docker 自动创建属主 root 的目录。Dockerfile 容器内 /opt/app/ 全保留不动。 | `skills/dev/project-init` |
| 2026-07-13 15:10 +0800 | `project-init` | `0.1.2` | `dev` | 修复 | 可用 | Claude Code / Codex / Generic | 修复 check-build-ready.sh.tmpl 5 缺陷：①L2/L3 改运行时从 .dev-flow.yml 动态读 build-credentials（不再生成时固化 {{var}}，用户回填配置后同步生效）；②L2 curl 加 -g 关 URL globbing（[] 否则调用失效致凭据误报不存在）；③L3 registry 强制 https（私有 registry 走 443，误填 scheme 自动归一化）；④report() printf 去多余双引号（原输出 ," 非法 JSON）；⑤脚本自动 cd 项目根（非项目根运行不再全红）。build-readiness 文档同步：硬编码 /opt/app 改 {{deploy.root}}，新增「配置同步须知」。 | `skills/dev/project-init` |
| 2026-07-13 14:43 +0800 | `project-init` | `0.1.1` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 初始化表单新增两项强制可配置项：`server.port`（服务启动端口，默认 8080(java-web)/8700(java-mcp)）与 `deploy.root`（部署根目录，默认 /opt/app）；留空时回退 manifest 默认。解决 DEPLOY_ROOT 硬编码 /opt/app 与实际服务器目录不符、端口无法在 init 阶段指定的问题。run.sh/rollback.sh/Jenkinsfile/Dockerfile 模板不变（变量来源链已支持）。 | `skills/dev/project-init` |
| 2026-07-10 13:30 +0800 | `project-init` | `0.1.0` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 新增 Java 项目脚手架生成器；template + mixin 独立模板架构（java-web / java-mcp），template 不继承、零 exclude，消除 javax/jakarta 残留；生成 .dev-flow.yml（含 scaffold 块 + build-credentials）并写项目级状态 .dev-flow/project.json，作为 dev-lifecycle 第 0 个 cascade 节点。 | `skills/dev/project-init` |
| 2026-07-10 13:30 +0800 | `dev-lifecycle` | `0.1.6` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 织入 project-init 为项目级 cascade 节点：新增项目级状态层 .dev-flow/project.json（schema project-state.schema.json）、Auto Cascade 0 项目级移交、Operation Contract 的 project-init:scaffold step、Phase 表与恢复表的 scaffold 分支、前置条件的骨架就绪检查；dev-flow.schema.json 新增 scaffold 块与 ci.jenkins.build-credentials；resolve-active-state.py 增 --scope project。 | `skills/dev/dev-lifecycle` |
| 2026-07-09 14:30 +0800 | `dev-lifecycle` | `0.1.5` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 新增多功能并行状态协议：状态文件按功能隔离到 `.dev-flow/states/<feature>.json`，活动指针 `.dev-flow/active` 标记当前开发功能；新增 `resolve-active-state.py`（resolve/set/switch/list/migrate），解析规则为分支为准 + 同步指针；schema 新增 `state` 配置块；完全向后兼容 `single` 旧单文件模式。 | `skills/dev/dev-lifecycle` |
| 2026-07-09 14:30 +0800 | `git-flow` | `0.1.5` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | `init-branch.sh` 在 per-feature 模式下把状态写到 `.dev-flow/states/<feature>.json` 并写入活动指针，支持多功能并行；`--state` 显式覆盖与 `single` 模式维持原行为；下游脚本由编排器传入解析后的 `--state` 路径。 | `skills/dev/git-flow` |
| 2026-07-09 14:30 +0800 | `dev-spec` | `0.1.4` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | intake 确定 feature slug 后先调 `resolve-active-state.py set` 建立活动状态文件与指针，再写入 spec/sources/implementation；状态路径由 resolver 解析，`single` 模式回退旧单文件。 | `skills/dev/dev-spec` |
| 2026-07-09 14:30 +0800 | `ci-trigger` | `0.1.4` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 文档明确状态文件路径由 dev-lifecycle resolver 解析后以 `--state` 传入，脚本默认 `.dev-flow-state.json` 兜底；脚本默认值与行为不变。 | `skills/dev/ci-trigger` |
| 2026-06-27 21:45 +0800 | `diverge-converge` | `0.1.0` | `creative` | 新增 | 可用 | Claude Code / Codex / Generic | 新增领域无关的扩散收敛思维方法，支持将半成熟想法、多轮探索或开放复杂课题沉淀为可实施、可交接的手稿。 | `skills/creative/diverge-converge` |
| 2026-06-17 18:44 +0800 | `dev-lifecycle` | `0.1.4` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 将 GUI merge 明确为默认关闭的可选辅助能力；开启后要求检测 IntelliJ IDEA 命令和 Git mergetool 配置，不可用时自动降级到文本冲突流程，不阻断 lifecycle 主流程。 | `skills/dev/dev-lifecycle` |
| 2026-06-17 18:44 +0800 | `git-flow` | `0.1.4` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 新增 `integration.conflict.gui-merge` 配置，默认关闭；开启后默认使用 `intellij` / `idea`，冲突报告输出检测结果和降级原因，并补充 IDEA 命令行与 mergetool 配置指南。 | `skills/dev/git-flow` |
| 2026-06-17 17:41 +0800 | `dev-lifecycle` | `0.1.3` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 收紧 Step Loop 为业务闭环级拆分，新增实时 `current-step` 状态同步脚本和一致性约束；Jenkins 构建失败后回到 code/step revising 并重新 review。 | `skills/dev/dev-lifecycle` |
| 2026-06-17 17:41 +0800 | `dev-spec` | `0.1.3` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 调整 implementation steps 拆分策略，禁止按 DTO、工具类、client、service、controller 等技术层过细拆分，改为按业务闭环、领域边界、外部系统或风险门禁拆分。 | `skills/dev/dev-spec` |
| 2026-06-17 17:41 +0800 | `ci-trigger` | `0.1.3` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 强化 Jenkins 长流程轮询、失败日志分析和 state 回修行为，新增可选钉钉通知脚本，通知通过环境变量 webhook 启用且默认关闭。 | `skills/dev/ci-trigger` |
| 2026-06-17 17:41 +0800 | `git-flow` | `0.1.3` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 补充冲突 GUI merge tool 提示和文本冲突标记处理规则，要求解决后扫描 `<<<<<<<` / `=======` / `>>>>>>>` 并通过 `git diff --check` 后才能继续。 | `skills/dev/git-flow` |
| 2026-06-16 15:23 +0800 | `dev-lifecycle` | `0.1.2` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 新增证据化 spec 消费和 Step Loop 协议，支持按 `implementation.steps` 单 feature 分支顺序开发、逐步 review 和跨会话恢复；worktree/step branch 明确保留到 V2。 | `skills/dev/dev-lifecycle` |
| 2026-06-16 15:23 +0800 | `dev-spec` | `0.1.2` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 支持从用户描述、HTTP 文档、本地 PDF/DOCX、API 文档和原型图整理证据化 spec，新增 API 对接模板、source manifest、复杂度分级和实施步骤推荐。 | `skills/dev/dev-spec` |
| 2026-06-16 15:23 +0800 | `git-flow` | `0.1.2` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 增加单 feature 分支内的 step-aware commit 约定，commit state 自动记录当前 implementation step；明确 V1 不创建 step branch 或 worktree。 | `skills/dev/git-flow` |
| 2026-06-16 11:43 +0800 | `dev-lifecycle` | `0.1.1` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 收紧 V1 编排边界为 Jenkins 测试部署，补充 operation contract、`integrated` phase 和状态恢复责任，移除默认 cascade 中未实现的 notify step。 | `skills/dev/dev-lifecycle` |
| 2026-06-16 11:43 +0800 | `dev-spec` | `0.1.1` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 调整触发边界，避免与 dev-lifecycle 的“开始开发”语义重叠；补充 required-sections 英文 slug 与中文 Markdown 标题的映射。 | `skills/dev/dev-spec` |
| 2026-06-16 11:43 +0800 | `ci-trigger` | `0.1.1` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 收紧能力声明为 V1 仅 Jenkins，修复 macOS YAML 解析问题，新增 state 更新，统一安全 JSON 输出并默认脱敏 Jenkins URL。 | `skills/dev/ci-trigger` |
| 2026-06-16 11:43 +0800 | `git-flow` | `0.1.1` | `dev` | 更新 | 可用 | Claude Code / Codex / Generic | 修复 macOS YAML 解析问题，新增 `.dev-flow-state.json` 更新、JSON 安全输出和集成冲突状态记录，明确冲突 V1 为分类报告而非自动应用。 | `skills/dev/git-flow` |
| 2026-06-12 21:30 +0800 | `dev-lifecycle` | `0.1.0` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 编排 dev-spec、git-flow、ci-trigger 全流程，含状态机协议、Review Loop（跨会话审查循环）、Auto Cascade（自动级联执行）、JSON Schema 和 Java+Maven+Jenkins 项目模板。 | `skills/dev/dev-lifecycle` |
| 2026-06-12 21:00 +0800 | `dev-spec` | `0.1.0` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 将需求对话转化为结构化 spec 文档，支持 default 和 minimal 两种模板，自动输出到项目 docs/specs 目录。 | `skills/dev/dev-spec` |
| 2026-06-12 20:30 +0800 | `ci-trigger` | `0.1.0` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 触发 CI 构建、轮询状态、拉取失败日志并分析原因，V1 实现 Jenkins 适配，含环境变量校验、配置验证、构建监控和失败日志提取。 | `skills/dev/ci-trigger` |
| 2026-06-12 20:00 +0800 | `git-flow` | `0.1.0` | `dev` | 新增 | 可用 | Claude Code / Codex / Generic | 管理 feature 分支创建、提交、推送、合并测试分支，支持冲突自动分类（4 种 trivial + business），V1 仅实现 merge-local 集成策略，优先适配 Gitee + Java/Maven + Jenkins。 | `skills/dev/git-flow` |
| 2026-06-14 20:12 +0800 | `wechat-markdown-publisher` | `0.2.4` | `creative` | 更新 | 可用 | Claude Code / Codex / Generic | 新增通用封面上传降级策略：本地封面可通过 `--cover-upload-command` 或 `WECHAT_MARKDOWN_COVER_UPLOAD_COMMAND` 尝试上传为公网 URL；命令缺失、失败、超时或无 URL 输出时自动降级为本地 `images/cover*.png`，不中断文章生成，并在报告中记录原因。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-14 20:04 +0800 | `wechat-markdown-publisher` | `0.2.3` | `creative` | 更新 | 可用 | Claude Code / Codex / Generic | 调整开头引用的渲染顺序：原文已有或 Agent 补写的引用仍只保留一份，但最终排在目录和封面图之后；预览页显示为 H1、目录、封面图、开头引用、正文，复制到公众号时为目录、封面图、开头引用、正文。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-14 18:57 +0800 | `wechat-markdown-publisher` | `0.2.2` | `creative` | 更新 | 可用 | Claude Code / Codex / Generic | 新增开头引用策略：原文开头已有 `>` 引用时保持不变；没有开头引用时，Agent 可生成约 100 字正文总结与阅读钩子，并通过 `--lead-quote` 或 `--lead-quote-file` 插入到 H1 后、目录前；报告记录引用状态。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-14 17:46 +0800 | `wechat-markdown-publisher` | `0.2.1` | `creative` | 更新 | 可用 | Claude Code / Codex / Generic | 优化公众号复制时的图片策略：公网图片复制时保留原 HTTPS/HTTP 链接并尝试备份到 `images/`，本地图片继续复制到 `images/` 用于预览，同时在复制公众号格式前提示本地图片无法在微信公众号中加载；README 增加 Typora 图片上传与七牛云命令示例。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-10 18:04 +0800 | `wechat-markdown-publisher` | `0.2.0` | `creative` | 更新 | 可用 | Claude Code / Codex / Generic | 新增默认 `x-tech-black` 科技黑 X 风格主题，H2 自动生成前置序号，目录自动编号，支持当前 Agent 生图 skill 插入无图注封面图；复制公众号格式时不复制 H1 标题，并可选本地 Python PDF 导出。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-02 14:22 +0800 | `wechat-markdown-publisher` | `0.1.0` | `creative` | 新增 | 可用 | Claude Code / Codex / Generic | 将 Typora/Markdown 文章转换为微信公众号可粘贴的多主题富文本，内置科技、生活、教育、医疗、餐饮和营销主题，并生成预览、复制入口、图片清单和兼容性检查报告。 | `skills/creative/wechat-markdown-publisher` |
| 2026-06-01 15:23 +0800 | `mafengwo-original-images` | `0.1.0` | `creative` | 新增 | 可用 | Claude Code / OpenClaw / Codex / Generic | 提取马蜂窝图片页或游记页原图链接，下载原图，生成 `链接,大小M` 清单，并支持断点续下与校验。 | `skills/creative/mafengwo-original-images` |
