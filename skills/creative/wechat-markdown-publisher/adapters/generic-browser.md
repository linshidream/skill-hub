# Generic Browser Adapter

Use this workflow when the agent has no dedicated browser automation.

1. Run the converter script.
2. Open the generated `preview.html` in a browser.
3. Click `复制公众号格式`.
4. Paste into the WeChat Official Account editor.
5. Compare the editor output with `preview.html`, especially headings, code blocks, tables, and images.
