# API 文档

启动应用后访问 OpenAPI JSON（原始格式，直接导入 Apifox 使用，不引入 UI 层）：

- **OpenAPI JSON**：{{swagger.api.url}}

> 用 springdoc-openapi（api-only，无 UI、无 Knife4j）。generic-web 用 1.x(Boot2)，mcp 用 2.x(Boot3)，统一输出 `/v3/api-docs`。springfox 已停更且与 Boot3 不兼容，弃用。
