{{project.name}}:
  env: prod
logging:
  file:
    path: /opt/app/logs   # 容器内路径（固定，与宿主机无关）；run.sh 挂载到宿主机 {{deploy.root}}/logs/<project>
  level:
    {{package}}: INFO
spring:
  # @@SPRING-EXTRA@@
# @@TOP-EXTRA@@
