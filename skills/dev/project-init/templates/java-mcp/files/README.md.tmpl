# {{project.name}}

> 由 `project-init` skill 生成（template=java-mcp，Spring AI MCP Server）。

## 项目结构

- **父 POM**：`{{project.groupId}}:{{project.name}}` `1.0.0-SNAPSHOT` (packaging=pom，纯聚合)
- **核心模块**：`{{core.module.name}}` (继承父，packaging=jar)
- **启动类**：`{{package}}.Application`（`@SpringBootApplication` exclude 了 Spring AI 的 SSE auto-config，改由 `{{package}}.config.McpStreamableServerConfig` 手动装配 Streamable HTTP 传输）
- **MCP 传输**：标准 Streamable HTTP，端点 `POST /mcp`（见下节）
- **finalName**：`{{core.module.name}}.jar` → 供 Dockerfile `ADD`
- **Java**：{{java.version}}（pom/jenkins maven镜像/docker运行镜像 四处对齐）
- **Spring Boot**：{{boot.version}} / **Spring AI**：{{spring-ai.version}}

## MCP 端点（Streamable HTTP）

现代 MCP 客户端（Claude Code / MCP Inspector / Codex）默认走 Streamable HTTP，POST 到 `/mcp`。

- 连接 URL：`http://localhost:{{server.port}}/mcp`
- 传输类型：Streamable HTTP（非 SSE；Spring AI 1.0.x auto-config 默认只装配 SSE，本骨架手动改为 Streamable）
- 示例工具：`{{package}}.mcp.ExampleTools#queryById`（`@Tool`，由 `McpStreamableServerConfig` 注册为 `ToolCallbackProvider`）

握手示例（curl）：
```
# 1. initialize（拿 Mcp-Session-Id）
curl -i -X POST http://localhost:{{server.port}}/mcp \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H 'MCP-Protocol-Version: 2025-06-18' \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"t","version":"1"}}}'

# 2. notifications/initialized（用上一步返回的 Mcp-Session-Id）
curl -i -X POST http://localhost:{{server.port}}/mcp \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H 'MCP-Protocol-Version: 2025-06-18' \
  -H 'Mcp-Session-Id: <上一步返回的 session id>' \
  -d '{"jsonrpc":"2.0","method":"notifications/initialized"}'

# 3. tools/list（返回 example_query）
curl -i -X POST http://localhost:{{server.port}}/mcp \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H 'MCP-Protocol-Version: 2025-06-18' \
  -H 'Mcp-Session-Id: <session id>' \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list"}'
```

> 新增工具：在 `{{package}}.mcp` 下加 `@Component`+`@Tool` 类，并在 `McpStreamableServerConfig#exampleToolCallbackProvider` 注册（Spring AI 1.0.x 无 tool-callback auto-config，`@Tool` 不自动注册）。

## 配置加载链路（关键，易踩坑）

容器运行时由 `run.sh` 注入：
```
--spring.profiles.active=${ACTIVE}
--spring.config.additional-location=file:${CONTAINER_CONFIG_DIR}/
```

**坑（已规避）**：
- 用 `additional-location`（追加，保留 jar 内默认），不用 `location`（替换，丢默认）。
- 外置 `spring.config.import` 的 `file:` 必须用绝对路径——相对路径解析到容器 CWD。

## 构建与部署

- 本地：`mvn -pl {{core.module.name}} -am clean package`
- CI：`deploy/jenkins/Jenkinsfile`
- 部署：`deploy/docker/run.sh {{core.module.name}} <version> <active>`
- 回滚：`deploy/docker/rollback.sh {{core.module.name}} <active>`（读 `.deploy-history`）
- 就绪检查：`scripts/check-build-ready.sh`（机器）+ `docs/checklist/build-readiness.md`（人工）

## 与 dev-lifecycle 衔接

- `.dev-flow.yml` 已生成。启动开发：`开始开发 <功能>`。
- spec 由 `dev-spec` 产出至 `docs/specs/`，本 skill 只建空目录。
- 分支：`master` / `test` / `feat/{developer}/{feature}`。
