# Claude Code Notes

Claude Code 进入本仓库时，请先阅读并遵循 [AGENTS.md](AGENTS.md)。

本仓库维护的是跨 agent 可复用的 skill，不是 Claude Code 专属 skill 集合。新增或修改 skill 时，请保持 `SKILL.md` agent-neutral，并同步更新 `SKILL_RELEASES.md` 与 `registry.json`。
